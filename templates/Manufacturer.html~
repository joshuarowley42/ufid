{% extends "base.html" %}
{% block title %}E3D Filament Database - Filament Details{% endblock %}
{% block content %}
<div class="container">
	<div class="jumbotron">
		<h3>{{ Manufacturer.Name }}</h3>
	</div>
	
   
	<div class="row">
		<div class="col-sm-12">
			<h3>Filaments</h3>
			<table class="table table-striped">
			<tr>
				<th>Filament ID</th><th>Name</th><th>Diameter</th><th>MPN</th>
			</tr>
			{% for Filament in Filaments %}
			<tr>
				<td><a href="/Filament/{{ Filament.FilamentID }}">{{ Filament.FilamentID }}</a></td>
				<td><a href="/Filament/{{ Filament.FilamentID }}">{{ Filament.Name }}</a></td>
				<td>{{ Filament.Diameter }}</td><td>{{ Filament.MPN }}</td>
			</tr>
			{% endfor %}
			</table>
		</div>
	</div>

</div>
{% endblock %}


